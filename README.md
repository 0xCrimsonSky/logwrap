# LOGWRAP

A lightweight C wrapper that can capture the logs written to stdout by the underlying process and write to a custom local file.

## USAGE
```
gcc -o app main.c
./app "node myprogram.js"
```